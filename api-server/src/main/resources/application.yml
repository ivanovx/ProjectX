server:
  port: 9000
spring:
  kafka:
    bootstrap-servers: localhost:9092
  data:
    mongodb:
      auto-index-creation: true
      database: projectX
      host: localhost
      port: 27017
      username: root
      password: root
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8000