server:
  port: 8005

spring:
  application:
    name: measurement-service
  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb://localhost:27417/measurementDb

eureka:
  client:
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka